<?xml version="1.0"?>
<PluginConfig>
	<Field id="pollingFrequency" type="textfield" defaultValue="5">
		<Label>Poll Email Servers Every (minutes):</Label>
	</Field>
	<Field id="messageNote" type="label" fontSize="small" fontColor="darkgray">
		<Label>The plugin will not poll if the frequency is set to zero.  Only enter zero if you want to set up your own polling schedule.  Maximum polling interval is 24 hours (1440 minutes).</Label>
	</Field>
	<Field id="showDebugInfo" type="checkbox">
		<Label>Enable debugging:</Label>
		<Description>(not recommended)</Description>
	</Field>
</PluginConfig>
